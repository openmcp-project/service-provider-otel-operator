name: REUSE Compliance Check

on: [push, pull_request]

permissions:
  contents: read

jobs:
  run_reuse:
    uses: openmcp-project/build/.github/workflows/reuse.lib.yaml@e6fddacd119db2136c40f9d5cca6a9ce3920b435 # main
    secrets: inherit
